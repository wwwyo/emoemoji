{
  "rules": {
    "quotes": ["error", "double"],
    "vue/multi-word-component-names": "off"
  },

  "overrides": [
    {
      "files": ["bin/*.js", "lib/*.js"],
      "excludedFiles": "*.test.js",
      "rules": {
        "quotes": ["error", "single"]
      }
    }
  ]
}
